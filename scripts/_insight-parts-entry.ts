/** Bundle entry so the test can reach both shared modules in one import. */
export { explainScore, opportunityScore } from '../shared/opportunity';
export { disambiguateProductNames, shortProductName } from '../shared/product-name';
